apiVersion: v1
kind: Service
metadata:
  name: profile-app
spec:
  type: NodePort
  selector:
    app: profile-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30090
